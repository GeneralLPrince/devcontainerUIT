{
    "name": "python-c-dev",
    "image": "mcr.microsoft.com/vscode/devcontainers/python:3.11", // Base image with Python
    "customizations": {
        "vscode": {
            "extensions": [
                "ms-python.python",       // Python extension
                "ms-vscode.cpptools"      // C/C++ extension for VS Code
            ]
        }
    },
    "features": {
        "ghcr.io/devcontainers/features/common-utils:1": {}, // Common utilities for development
        "ghcr.io/devcontainers/features/gcc:1": {}          // GCC for C/C++ development
    },
    "postCreateCommand": "pip install -r requirements.txt", // Optional: Python dependencies
    "settings": {
        "terminal.integrated.shell.linux": "/bin/bash"       // Use Bash as the default terminal
    }
}
